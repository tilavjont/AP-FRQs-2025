public sumOrSumGame(int numRows, int numCols) {
  int arr[][] = new int[numRows][numCols];
  for (int i = 0; i < numRows; i++) {
    for (int j = 0; j < numCols; j++) {
      arr[i][j] = ((int)(Math.Random()*9 + 1);
    }
  }

  
